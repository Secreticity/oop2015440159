
public class Point {

	public double x;
	public double y;
	
	public Point move(Point p) {
		double a, b;  //Temporary Variables

		//C : P2 = move(p1, p);
		//Java: P2 = P1.move(p);
		
		//포인트 p  만큼 이동
		a = this.x + p.x;
		b = this.y + p.y;

		//새로운 포인트 생성
		Point pnt = new Point();
		pnt.x = a;
		pnt.y = b;
		
		//새로운 포인트 반환
		return pnt;
	}
}
